<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <!-- Override library defaults — this is an application, not a library -->
        <GenerateDocumentationFile>false</GenerateDocumentationFile>
        <Nullable>enable</Nullable>
        <NoWarn>$(NoWarn);IDE0005</NoWarn>
        <RootNamespace>Npgsql.Sample</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../../../src/EntityFrameworkCore.Npgsql/ArchPillar.Extensions.EntityFrameworkCore.Npgsql.csproj" />
    </ItemGroup>

</Project>
