<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <RootNamespace>Acme.WasmContracts</RootNamespace>
    <AssemblyName>Acme.WasmContracts</AssemblyName>
    <!-- Deliberately a plain class library (Microsoft.NET.Sdk, not .Razor): a contracts/model project that
         annotates its types but has no Razor content. Its catalogs must still reach a WebAssembly client. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\src\Localization\ArchPillar.Extensions.Localization.csproj" />
  </ItemGroup>

</Project>
