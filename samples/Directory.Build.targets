<Project>

  <!-- Prevents the root Directory.Build.targets from injecting analyzer PackageReferences
       into sample projects that opt out of central package management. -->

  <PropertyGroup>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

</Project>
